{
  "name": "malamoneyball-football",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "getcontestresults": "node --env-file=.env --import tsx src/cli.ts > contest.json",
    "start": "node --import tsx src/cli.ts",
    "test": "node --import tsx --test test/draftkings.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "zod": "^4.6.5"
  }
}
